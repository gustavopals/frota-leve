import type { Routes } from '@angular/router';
import { MaintenancePage } from './pages/maintenance-page/maintenance-page';

export const MAINTENANCE_ROUTES: Routes = [{ path: '', component: MaintenancePage }];

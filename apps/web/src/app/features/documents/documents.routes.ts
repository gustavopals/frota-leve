import type { Routes } from '@angular/router';
import { DocumentsPage } from './pages/documents-page/documents-page';

export const DOCUMENTS_ROUTES: Routes = [{ path: '', component: DocumentsPage }];
